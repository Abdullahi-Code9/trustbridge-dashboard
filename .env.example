# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# GitHub org slug for maintainer dashboard access (e.g. "stellar" or your org name)
GITHUB_MAINTAINER_ORG=

# PostgreSQL connection string (Neon, Supabase, Vercel Postgres, etc.)
DATABASE_URL=postgresql://user:password@localhost:5432/trustbridge

# Stellar Horizon API (mainnet or testnet)
NEXT_PUBLIC_HORIZON_URL=https://horizon.stellar.org

# Default asset to check for trustline readiness (USDC on Stellar mainnet)
NEXT_PUBLIC_DEFAULT_ASSET_CODE=USDC
NEXT_PUBLIC_DEFAULT_ASSET_ISSUER=GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX5IHOWEBMGJI55ITFSZ6

# Minimum XLM balance considered "ready" (Stellar base reserve + headroom)
NEXT_PUBLIC_MIN_XLM_BALANCE=1

# Optional: Soroban contract ID for on-chain registry (future)
SOROBAN_CONTRACT_ID=
